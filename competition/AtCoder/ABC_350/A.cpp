#include <bits/stdc++.h>
using namespace std;
int main () {
    char a;
    cin >> a >> a >> a;
    int n;
    cin >> n;
    if (1 <= n && n < 350 && n != 316 )
        cout << "Yes" << endl;
    else
        cout << "No" << endl;
    return 0;
}